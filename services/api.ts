/**
 * Integración con ThingSpeak REST API para datos históricos
 * (Actualmente solo un placeholder para futura implementación)
 */

export const ApiService = {
  getHistoricoLlenado: async (contenedorId: string) => {
    console.log(`Obteniendo historial para ${contenedorId} desde ThingSpeak...`);
    // TODO: fetch a ThingSpeak
    return [];
  }
};
